language: r
sudo: false
cache: packages
r:
  - oldrel
  - release
  - devel

r_github_packages: krlmlr/ulimit

after_success:
  - test $TRAVIS_R_VERSION_STRING = "release" && Rscript -e 'covr::codecov()'
  
addons:
  apt_packages:
    - pandoc
